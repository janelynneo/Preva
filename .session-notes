# Session Notes — 2026-05-20

## Where we are

MetaboApp is now on its own GitHub repo (https://github.com/janelynneo/MetaboApp). All prior uncommitted changes committed and pushed. App running via Expo Go at `exp://192.168.1.204:8081`.

## Read first

1. `MetaboApp/screens/WelcomeScreen.tsx` — home screen with desk stretches timer (added this session)
2. `MetaboApp/screens/OnboardingScreen.tsx` — fixed "Start My Baseline" blank screen (submitting guard + try/catch)
3. `MetaboApp/services/notifications.ts` — seated nudges now distributed Mon–Fri (was hardcoded Tuesday)
4. `MetaboApp/CLAUDE.md` — full app overview, key files, design tokens

## In-flight state

- All prior uncommitted changes: committed as `2920f53 feat(Pulse): desk stretches with timer, onboarding fix, seated nudges Mon-Fri`
- Git remote redirected from Preva.git → MetaboApp.git; all 12 commits pushed

## Outstanding ledger (forest)

Forest empty — every item closed or externally blocked.

## Traps

- Ollama must be at `192.168.1.204:11434` — AI coach fails silently if unreachable; restart with `OLLAMA_HOST=0.0.0.0 open -a Ollama`
- Phone shows old bundle: force-quit Expo Go and reconnect
- HealthKit fetch timeout still missing in `services/health.ts` (same 20s pattern as aiCoach.ts)
- Team service is mock-only — no real backend
- Package version warnings in `npm install` output (several expo deps ahead of expected)
