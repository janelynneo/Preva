# Session Notes — 2026-05-20

## Where we are

App running with Metro on port 8081. AI coach rewritten with proper timeout + error messages, streak logic corrected, ambient music player added. All changes uncommitted.

## Read first

1. `MetaboApp/.session-notes` — this file
2. `MetaboApp/CLAUDE.md` — quick start, key files, design tokens
3. `MetaboApp/services/aiCoach.ts` — AI coach implementation (just fixed)

## In-flight state

All uncommitted. Modified files:
- `services/audio.ts` — NEW: expo-av background music service
- `components/MusicPlayer.tsx` — NEW: ambient music player UI component
- `services/config.ts` — NEW: explicit Ollama URL/model (no env fallback)
- `services/aiCoach.ts` — rewritten: 20s timeout, AbortController, specific error messages, history in context
- `screens/CheckInScreen.tsx` — streak bug fixed: uses getLastCheckInDate() not signupDate, same-day no-op
- `screens/WelcomeScreen.tsx` — MusicPlayer integrated + AMBIENT SOUND section
- `package.json` — added expo-av

## Traps

- Ollama must be at 192.168.1.204:11434 — AI coach fails silently if unreachable
- Expo Go QR for dev — full HealthKit needs `npx expo run:ios` native build
- `HomeScreen.tsx` is dead code (unused import in App.tsx already cleaned)

## Open questions for the human

- HealthKit service (`services/health.ts`) still needs a fetch timeout (same pattern as aiCoach)
- Team service is mock-only — no real backend
- Package version warnings in `npm install` output — several expo deps ahead of expected
