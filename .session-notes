# Session Notes — 2026-05-15

## Where we are

MetaboApp iOS build is working — clean compile, app running on iPhone 17 Pro simulator. Pod install path, Swift React imports, iOS 16 availability guards, and Ollama env-var config all fixed this session. No uncommitted changes remain.

## Read first

1. `MetaboApp/services/aiCoach.ts` — Ollama integration (OLLAMA_URL/MODEL now from env vars with localhost fallback)
2. `MetaboApp/ios/Podfile` — check pod path: `./LocalPods/MetaboHealthKit` (was wrongly `../LocalPods/`)
3. `MetaboApp/ios/LocalPods/MetaboHealthKit/MetaboHealthKit.podspec` — placeholder `s.source = { :git => "" }` to satisfy CocoaPods
4. `MetaboApp/ios/LocalPods/MetaboHealthKit/Sources/MetaboHealthKit.swift` — `import React`, iOS 16 `#available` guards on sleep analysis
5. `MetaboApp/.env.example` — Ollama config template (OLLAMA_URL, OLLAMA_MODEL)
6. `MetaboApp/ios/MetaboApp/MetaboApp-Bridging-Header.h` — React headers imported

## In-flight state

- All changes committed. App builds and runs.
- `.env` not yet created from `.env.example` — user needs to copy and configure OLLAMA_URL if connecting to a remote Ollama

## Traps

- `OLLAMA_URL` in `aiCoach.ts` defaults to `localhost:11434` — simulators on a Mac with local Ollama will work; remote Ollama needs `.env` with the correct IP
- `@anthropic-ai/sdk` removed from `package.json` but `package-lock.json` still contains the old lock — run `npm install` on a fresh clone to regenerate
- HealthKit on iOS Simulator returns empty data (no real Apple Watch) — `getWeeklySteps()` etc. will return empty arrays; fallback to cached AsyncStorage data kicks in

## Open questions for the human

- Should `OLLAMA_URL` default to the user's machine IP or stay `localhost` with a comment to change it?
- Any other native modules to add beyond MetaboHealthKit?
